<p id="notice"><%= notice %></p>

<h1>Listing Kmsaves</h1>

<table>
  <thead>
    <tr>
      <th>Disk number</th>
      <th>Sector</th>
      <th>Sector number l</th>
      <th>Data l</th>
      <th>Data h</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @kmsaves.each do |kmsafe| %>
      <tr>
        <td><%= kmsafe.disk_number %></td>
        <td><%= kmsafe.sector_id %></td>
        <td><%= kmsafe.sector_number_l %></td>
        <td><%= kmsafe.data_l %></td>
        <td><%= kmsafe.data_h %></td>
        <td><%= link_to 'Show', kmsafe %></td>
        <td><%= link_to 'Edit', edit_kmsafe_path(kmsafe) %></td>
        <td><%= link_to 'Destroy', kmsafe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Kmsave', new_kmsafe_path %>
